#include <stdio.h>
int main ()
{

    // #include <stdio.h> used to run code
    // #include <stdlib.h> used for system("cls"); -- FOR CLEANING THE TERMINAL
    // include <unistd.h> used for sleep() function
    // include <windows.h> used for Sleep() function also but in milliseconds

    //define p printf = used to make a shortcut for printf() = p()
    //define s scanf = used to make a shortcut for scanf() = s()




    return 0;
}