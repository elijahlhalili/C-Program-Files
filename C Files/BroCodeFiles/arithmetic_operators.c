#include <stdio.h>

int main ()
{

    // arithmetic operators = + - * / % ++ --

    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y
    //z = x - y
    //z = x * y
    //z = x / y
    //z = x % 2     - % (modulus operator) is used to calculate remainder of an integer division.
    //x++ means you are incrementing by 1 (adding by 1)
    //x-- means you are decrementing by 1 (subtracting by 1)
    //x+=2 means incrementing by 2 (adding)
    //x-=2 means decrementing by 2 (subtracting)
    //x*=2 means multiplying it by 2
    //x/=2 means diving it by 2
    
    

    printf("%d", x);

    return 0;
}